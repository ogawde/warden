steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - services/worker/Dockerfile
      - -t
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
substitutions:
  _IMAGE: us-central1-docker.pkg.dev/warden-hackathon/warden/warden-worker:latest
